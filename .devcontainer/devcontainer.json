{
  "name": "Python 3 & Streamlit Dev",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm", // 升級到 3.12，穩定且支援最新 Streamlit
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "streamlit_app.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  // 容器建立後跑一次：建立 venv + 安裝套件
  "postCreateCommand": "python -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  // 每次 attach (開啟 VS Code) 時在背景啟動 Streamlit（避免阻塞終端）
  "postAttachCommand": "bash -lc 'pgrep -f \"streamlit run streamlit_app.py\" >/dev/null || nohup .venv/bin/streamlit run streamlit_app.py --server.address 0.0.0.0 --server.port 8501 --server.enableCORS false --server.enableXsrfProtection false --browser.gatherUsageStats false >/tmp/streamlit.log 2>&1 &'",
  "forwardPorts": [
    8501
  ],
  "portsAttributes": {
    "8501": {
      "label": "Streamlit App",
      "onAutoForward": "notify" // 避免在容器內呼叫 gio 開啟瀏覽器
    }
  }
}